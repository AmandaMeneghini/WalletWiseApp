import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  centered: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  text: {
    position: 'absolute',
    fontSize: 32,
    fontWeight: 'bold',
    color: '#3b3dbf',
    textAlign: 'center',
  },
});
